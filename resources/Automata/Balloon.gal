Balloon (Init) {
	* (Init) :
		|Key(n) ? Pop : ()
}

Balloon2 (Init) {
	* (Init) : 
		| True ? Move(NE) / Move(NW) / Move(SE) / Move(SW) : (Moving)
	* (Moving) :
		| !(GotPower) ? Egg(F) : (Die)
		| Cell(F,V) ? Move(F) : (Moving)
		| Cell()? Turn(L) : (Moving)
	*(Die)
		| True ? Pop : ()
}