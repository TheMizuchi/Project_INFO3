MoveKeysArrows (Init) {
	* (Init):
		| Key(FU) ? Move(N) : (Init)
		| Key(FL) ? Move(W) : (Init)
		| Key(FD) ? Move(S) : (Init)
		| Key(FR) ? Move(E) : (Init)
		| Key(r) ? Pick : (Init)
		| Key(ENTER) ? Wizz() : (Init)
		| Key(x) ? Pop : (Init)
}