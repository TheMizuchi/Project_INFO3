MoveKeys (Init) {
	* (Init):
		| Key(z) ? Move(N) : (Init)
		| Key(q) ? Move(W) : (Init)
		| Key(s) ? Move(S) : (Init)
		| Key(d) ? Move(E) : (Init)
		| Key(t) ? Pick : (Init)
		| Key(a) ? Hit : (Init)
		| Key(SPACE) ? Wizz : (Init)
}